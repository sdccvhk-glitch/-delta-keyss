const form=document.getElementById('statusForm'),idEl=document.getElementById('orderId'),result=document.getElementById('result'),err=document.getElementById('error');const preset=new URLSearchParams(location.search).get('order');if(preset)idEl.value=preset;form.addEventListener('submit',async e=>{e.preventDefault();err.textContent='';try{const id=idEl.value.trim();const r=await fetch('/api/orders/'+encodeURIComponent(id));const out=await r.json();if(!r.ok)throw Error(out.error||'Order not found');const o=out.order;result.hidden=false;result.innerHTML=`<div class="eyebrow">ORDER ${esc(o.id)}</div><h2>${esc(o.productName)}</h2><p class="muted">Status: <b>${esc(o.status).toUpperCase()}</b></p>${o.status==='paid'?`<div class="payment-box"><div class="eyebrow">YOUR KEY</div><h2>${esc(o.key)}</h2></div>`:o.status==='rejected'?'<p class="muted">This order was rejected. Please contact support if you believe this is incorrect.</p>':'<p class="muted">Payment submitted. An admin must verify your UPI transaction before the key is released.</p>'}`}catch(x){result.hidden=true;err.textContent=x.message}});function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
